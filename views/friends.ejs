<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            background-color: #f5f5f5;
        }

        .friends-bar-container {
            max-width: 1000px;
            margin: 20px auto;
            padding: 10px;
            background-color: #fff;
            border: 1px solid #ddd;
            border-radius: 5px;
            overflow-x: auto;
        }

        table {
            width: 100%;
            border-collapse: collapse;
        }

        .btn-bar {
            background-color: #007bff;
            color: white;
            border: none;
            padding: 10px 20px;
            margin: 5px 0;
            cursor: pointer;
            border-radius: 5px;
            font-size: 1em;
        }

        .search {
            padding: 5px;
            border: 1px solid #ccc;
            border-radius: 5px;
            margin-right: 10px;
        }

        .btn-search {
            background-color: #28a745;
            color: white;
            border: none;
            padding: 5px 10px;
            cursor: pointer;
            border-radius: 5px;
        }

        .friends-navigation {
            max-width: 1000px;
            margin: 20px auto;
            padding: 10px;
            background-color: #fff;
            border: 1px solid #ddd;
            border-radius: 5px;
        }

        .orang-lain-card {
            border: 1px solid #ccc;
            padding: 10px;
            border-radius: 5px;
            margin-bottom: 10px;
        }

        .username-place {
            font-weight: bold;
        }

        .others-bar {
            display: block;
            margin-top: 10px;
            text-decoration: none;
            font-size: 1em;
        }

        @media (max-width: 768px) {
            .btn-bar, .btn-search, .search {
                width: 100%;
                margin: 5px 0;
            }

            .friends-bar-container, .friends-navigation {
                margin: 10px;
                padding: 10px;
            }

            table {
                display: block;
            }

            tr {
                display: flex;
                flex-direction: column;
                align-items: stretch;
            }

            td {
                display: block;
                margin: 5px 0;
            }
        }
    </style>
</head>
<body>
    <div class="friends-bar-container">
        <table>
            <tr>
                <td>
                    <a href="/first-page/<%= userId %>" style="text-decoration: none;">
                        <button class="btn-bar" style="color:blue"><%= username %></button>
                    </a>
                </td>
                <td>
                    <a href="/list-chat-open/<%= userId %>" style="text-decoration: none;">
                        <button class="btn-bar">chatting</button>
                    </a>
                </td>
                <td>
                    <a href="/beranda/<%= userId %>" style="text-decoration: none;">
                        <button class="btn-bar">navigasi</button>
                    </a>
                </td>
                <td>
                    <a href="/friends-list/<%= userId %>" style="text-decoration: none;">
                        <button class="btn-bar">&larr;</button>
                    </a>
                </td>
                <td>
                    <a href="/orang-lain/<%= userId %>" style="text-decoration: none;">
                        <button class="btn-bar">cari kenalan</button>
                    </a>
                </td>
                <td>
                    <form action="/search-friends" method="post">
                        <input type="hidden" value="<%= userId %>" name="user_id">
                        <input type="text" name="input_search" placeholder="cari siapa" class="search">
                        <button type="submit" class="btn-search">cari</button>
                    </form>
                </td>
            </tr>
        </table>
    </div>
    <div class="friends-navigation">
        <% friends.forEach(element => { %>
            <fieldset class="orang-lain-card">
                <legend class="username-place"><%= element.user_name %></legend>
                <a href="/others-content/<%= element.user_id %>/<%= userId %>" class="others-bar" style="color:gray">konten</a>
                <a href="/open-chat-system/<%= userId %>/<%= element.user_id %>" class="others-bar" style="color:green">buka chat</a>
            </fieldset>
        <% }) %>
    </div>
</body>
</html>
