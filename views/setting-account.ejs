<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            background-color: #f5f5f5;
        }

        .account-sett-wrapper {
            max-width: 600px;
            margin: 20px auto;
            padding: 20px;
            background-color: #fff;
            border: 1px solid #ddd;
            border-radius: 5px;
        }

        fieldset {
            border: 1px solid #ccc;
            padding: 10px;
            margin-bottom: 20px;
            border-radius: 5px;
        }

        legend {
            padding: 0 10px;
        }

        .username-fields, .password-fields {
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .location {
            display: flex;
            flex-direction: column;
        }

        .location b {
            margin-bottom: 10px;
        }

        .location-update {
            width: 100%;
            height: 100px;
            padding: 10px;
            border: 1px solid #ccc;
            border-radius: 5px;
            margin-bottom: 10px;
            resize: vertical;
            color:#007bff
        }

        .location button {
            background-color: #007bff;
            color: white;
            border: none;
            padding: 10px;
            border-radius: 5px;
            cursor: pointer;
            font-size: 1em;
        }

        .option-get {
            display: flex;
            justify-content: space-between;
            max-width: 600px;
            margin: 20px auto;
            padding: 20px;
        }

        .btn-bar {
            padding: 10px 20px;
            border: 1px solid transparent;
            border-radius: 5px;
            cursor: pointer;
            font-size: 1em;
        }

        @media (max-width: 768px) {
            .username-fields, .password-fields {
                flex-direction: column;
                align-items: flex-start;
            }

            .username-value, .password-value {
                margin-top: 10px;
            }

            .option-get {
                flex-direction: column;
                align-items: center;
            }

            .btn-bar {
                width: 100%;
                margin-bottom: 10px;
            }
        }
    </style>
</head>
<body>
    <div class="account-sett-wrapper">
        <input type="hidden" name="old_UserId" value="<%= account.account_id %>">

        <fieldset class="username-fields">
            <legend style="color:rgb(79, 113, 29)">username</legend>
            <span class="username-value" id="username-value"><%= account.username %></span>
        </fieldset>
        <fieldset class="password-fields">
            <legend style="color:brown">password</legend>
            <span class="password-value"><%= account.password %></span>
        </fieldset>

        <form action="/update-data/<%= account.account_id %>" method="post" class="location">
            <b style="color:rgb(99, 48, 149);font-family: 'Franklin Gothic Medium', 'Arial Narrow', Arial, sans-serif;">location</b>
            <input type="hidden" name="username" value="<%= account.username %>">
            <input type="hidden" name="password" value="<%= account.password %>">
            <textarea name="location_update_input" class="location-update"><%= account.location %></textarea>
            <button>update</button>
        </form>
    </div>

    <div class="option-get">
        <a href="/mulut.com"><button class="btn-bar" style="color:rgb(227, 201, 201);border-color:rgb(210, 193, 193);background-color: rgb(174, 143, 143);">log-out</button></a>
        <a href="/first-page/<%= account.account_id %>"><button class="btn-bar" style="color:rgb(125, 96, 52);border-color:rgb(187, 39, 163)">kembali</button></a>
    </div>
</body>
</html>
